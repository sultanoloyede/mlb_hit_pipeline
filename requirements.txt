# EL
dlt[motherduck]>=1.0

# T
dbt-duckdb>=1.8

# sources (Phase 1)
pybaseball>=2.2
MLB-StatsAPI>=1.7
pandas>=2.0
pyarrow>=14.0

# ML plane (Phase 4)
# scikit-learn/numpy/lightgbm are PINNED to the training environment:
# models/production/ is a joblib pickle and must be unpickled by the
# exact versions that wrote it (Actions failed on 1.7.x internals).
# When you upgrade locally: retrain, re-export, and bump these together.
scikit-learn==1.6.1
numpy==1.26.4
lightgbm==4.6.0
joblib==1.5.3
mlflow>=2.9
PyYAML>=6.0
matplotlib>=3.7
