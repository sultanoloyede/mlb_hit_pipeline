name: mlb_hits
version: "0.1.0"
profile: mlb_hits

model-paths: ["models"]
test-paths: ["tests"]

models:
  mlb_hits:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
    ml:
      +materialized: table
      +schema: ml
    output:
      # predictions are append-only; models here become incremental in Phase 6
      +materialized: table
      +schema: output
